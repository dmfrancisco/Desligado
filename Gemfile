source 'http://rubygems.org'

gem 'rails', '3.0.5'
gem 'sqlite3-ruby', '1.2.5', :require => 'sqlite3'

gem 'jquery-rails'
gem "rack-offline" # Generate the application manifest
gem "jammit", :git => 'git://github.com/documentcloud/jammit.git' # Asset packaging library

group :development, :test do
  gem "annotate" # annotate your models running $ annotate --exclude tests,fixtures
  gem "hirb" # Improve the IRB
end
